"""Offline end-to-end pipeline runner (ingest -> shortlist -> dispatch -> level ->
recommend), forced DEMO_MODE, readable trace at each stage.

Rebuilt in Phase 7 once the stage bodies exist. Kept as a placeholder so the
package layout is stable.
"""

if __name__ == "__main__":
    print("run_pipeline is rebuilt in Phase 7 (SiteSource pivot); stages are stubs until then.")
