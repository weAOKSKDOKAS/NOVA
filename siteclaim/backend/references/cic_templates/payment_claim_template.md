# Payment Claim — compliant skeleton (Layer 3 template)

> ⚠️ **UNVERIFIED template.** Generic skeleton of a SOPO payment claim with the
> mandatory particulars as placeholders. The exact **s.18 content requirements**
> are still **TODO** (to be confirmed from the Cap.652 text), so the placeholder
> set below is provisional. It is **not legal advice** and not an official CIC
> form. Confirm required wording and layout against the enacted Ordinance and
> current CIC guidance before use. Placeholders are written as
> `{{double_braces}}` and map to fields on `schemas.models.ClaimDraft` /
> `ExtractedFacts`.

---

## PAYMENT CLAIM

**Made under the Construction Industry Security of Payment Ordinance (Cap. 652)** # UNVERIFIED — confirm exact statutory wording

**Claim reference:** `{{claim_reference}}`
**Date of this claim:** `{{claim_served_date}}`
**Reference date:** `{{reference_date}}`  <!-- ExtractedFacts.reference_date -->

### 1. Parties

| | Name | Role | Address |
| --- | --- | --- | --- |
| **Claimant** | `{{claimant_name}}` | `{{claimant_role}}` | `{{claimant_address}}` |
| **Respondent** | `{{respondent_name}}` | `{{respondent_role}}` | `{{respondent_address}}` |

### 2. The contract

- **Contract identification:** `{{contract_reference}}`
- **Contract type:** `{{contract_type}}`
- **Contract sum:** `{{currency}} {{contract_sum}}`
- **Work period covered by this claim:** `{{work_period_start}}` to `{{work_period_end}}`

### 3. Amount claimed

> **Total claimed (this claim): `{{currency}} {{claimed_amount}}`**

### 4. Description of work / goods / services and basis of calculation

`{{basis_of_calculation}}`

| # | Description | Qty | Unit | Rate | Amount |
| --- | --- | --- | --- | --- | --- |
| 1 | `{{line_item_1_description}}` | `{{qty}}` | `{{unit}}` | `{{rate}}` | `{{amount}}` |
| … | … | … | … | … | … |

**Less amounts previously certified / paid:** `{{currency}} {{certified_to_date}}`
**Net amount now due:** `{{currency}} {{net_claimed_amount}}` # UNVERIFIED — confirm whether the claim states gross or net

### 5. Supporting documents

`{{supporting_doc_refs}}`  <!-- e.g. invoices, site records, measurement sheets -->

### 6. Statutory statement

> This payment claim is made under the Construction Industry Security of Payment
> Ordinance. `{{statutory_statement}}` # UNVERIFIED — exact mandatory statement to be confirmed

### 7. Service

- **Served on:** `{{respondent_name}}`
- **Method of service:** `{{service_method}}` # UNVERIFIED — must be a permitted method
- **Date served:** `{{claim_served_date}}`

---

*Generated by SiteClaim as a draft for human review (Stage 05). Not served until
approved. Statutory parameters are unverified placeholders — see the project
`CLAUDE.md`.*
